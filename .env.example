# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DATABASE_PATH=./telemetry.db

# Agent Configuration (for reference)
# Set DRIVER_NAME environment variable when running agent
# Set SERVER_URL in agent.py to point to this server
